# פריסה ל-Vercel

הקוד מוכן לפריסה ל-Vercel: יש בו נקודת כניסה מסוג WSGI (`api/index.py`),
קובץ הגדרות (`vercel.json`), ורשימת ספריות (`requirements.txt` בשורש).

> **לפני שמתחילים — נקודה חשובה:** Vercel בנוי ל-**Serverless (ללא מצב)**.
> הכלי שומר "מצב ריצה" בזיכרון (הטבלה שנטענה והשורות התקינות שנשמרות בשרת בין
> שלב הטעינה לשלב ההפקה). ב-Vercel מצב זה עלול להיאבד כשבקשות שונות מנותבות
> למופעים שונים — במיוחד בקבצים גדולים או בכמה משתמשים במקביל. לזרימה של משתמש
> יחיד ובקבצים קטנים זה בדרך כלל יעבוד, אבל **להדגמה יציבה ורב-משתמשית Render
> מתאים יותר** (ראה `file-loader/DEPLOY.md`). ההגדרות ל-Vercel כאן עושות את
> המרב: כותבות ל-`/tmp` (מערכת הקבצים היחידה שניתנת לכתיבה ב-Vercel).

---

## פריסה דרך אתר Vercel (הדרך הקלה — ללא שורת פקודה)

1. היכנס ל-https://vercel.com והירשם עם חשבון הגיטהאב שלך (**Continue with GitHub**).
2. בלוח הבקרה לחץ **Add New… → Project**.
3. בחר את המאגר **`Maoro97/MCP`** ולחץ **Import**.
4. במסך ההגדרות:
   - **Root Directory:** השאר את ברירת המחדל (שורש המאגר) — אל תשנה ל-`file-loader`,
     מפני שקובצי ההגדרה (`vercel.json`, `requirements.txt`) נמצאים בשורש.
   - **Framework Preset:** יזוהה אוטומטית לפי `vercel.json` (Other/Python) — אין
     צורך לשנות.
5. לחץ **Deploy** והמתן 1–3 דקות.
6. בסיום תתקבל כתובת ציבורית כמו:
   `https://mcp-<something>.vercel.app`
   זו הכתובת שתשתף — אנשים ייכנסו אליה מהדפדפן.

**עדכונים אוטומטיים:** כל דחיפת קוד לענף המחובר תפרוס מחדש אוטומטית.

---

## פריסה דרך שורת פקודה (אופציונלי)

```bash
npm i -g vercel      # התקנת ה-CLI (פעם אחת)
cd C:\Users\User\MCP # שורש המאגר (המקום עם vercel.json)
vercel               # פריסת תצוגה מקדימה — עקוב אחר השאלות
vercel --prod        # פריסה לכתובת הסופית
```

---

## מה הוגדר עבור Vercel (לרקע)

- `api/index.py` — נקודת הכניסה; מוסיפה את `file-loader/` ל-path ומייצאת את
  אפליקציית Flask בשם `app` (Vercel מגיש דרכה).
- `vercel.json` — מפנה את כל הבקשות ל-`api/index.py`, ומצרף את כל קבצי
  `file-loader/**` (מיפויים, קטלוגים, טבלאות מטבע, לוגו) ל-bundle.
- `requirements.txt` (בשורש) — הספריות ל-Vercel (pandas, openpyxl, pyyaml, flask).
- **כתיבה ל-`/tmp`:** הקוד מזהה את סביבת Vercel (משתנה `VERCEL`) ומפנה את תיקיית
  הפלט ל-`/tmp/fl-output`. אפשר לדרוס במשתנה סביבה `FILE_LOADER_OUTPUT`.

## מסך התחברות (הגנה בסיסמה)

הגנת הסיסמה המובנית של Vercel היא תכונה בתשלום (מסלול Pro). במקום זאת, לאפליקציה
יש **מסך התחברות מובנה** שמופעל דרך משתני סביבה — חינמי ועובד בכל פלטפורמה.

**איך מפעילים ב-Vercel:** Project → **Settings → Environment Variables**, והוסף:

| Key | Value | חובה? |
|-----|-------|-------|
| `APP_PASSWORD` | הסיסמה שתבחר | **כן** — בפריסת ענן (Vercel/Render) חובה, אחרת הגישה תיחסם |
| `SECRET_KEY` | מחרוזת אקראית ארוכה (לחתימת ה-cookie) | **מומלץ מאוד** — בלעדיו ההתחברות לא תישמר בין הרצות |
| `APP_USERNAME` | שם המשתמש (ברירת מחדל: `admin`) | לא |

ליצירת `SECRET_KEY` אקראי: הרץ `python -c "import secrets;print(secrets.token_hex(32))"`
והדבק את התוצאה.

לאחר ההוספה — פרוס מחדש (Deployments → Redeploy, או דחיפת commit). מעתה כל
כניסה לאתר תדרוש שם משתמש + סיסמה. יציאה: הקישור **🚪 יציאה** בראש הדף.

> **אבטחה (fail-closed):** בפריסת ענן (מזוהה לפי `VERCEL`/`RENDER`) — אם `APP_PASSWORD`
> **לא** מוגדר, האפליקציה **חוסמת את כל הגישה** ומציגה עמוד-הגדרה, במקום לשרת פתוח.
> כך שכחה של הסיסמה לא חושפת את המערכת. להרצה פתוחה מכוונת (למשל דמו) — הגדר
> `ALLOW_OPEN=1`. בהרצה מקומית (ללא הפלטפורמות הללו) האפליקציה נשארת פתוחה לנוחות.
>
> ⚠️ אם עד כה הסתמכת על ברירת-מחדל של `SECRET_KEY` — היא הוסרה (היוותה סיכון: מפתח
> ידוע מאפשר זיוף cookie). כעת **חובה** להגדיר `SECRET_KEY` כדי שההתחברות תישמר.

## מסד נתונים (היסטוריית טעינות) — שמירה קבועה ב-Vercel עם Postgres

היסטוריית הטעינות נשמרת במסד נתונים (כולל קובצי הפלט, לאחזור והורדה חוזרת ממסך
ההיסטוריה). הכלי תומך בשני מנועים אוטומטית:

- **Postgres** — כאשר מוגדר משתנה הסביבה `DATABASE_URL` או `POSTGRES_URL`.
  **זו הדרך לשמירה קבועה ב-Vercel** (מערכת הקבצים של Vercel זמנית).
- **SQLite** — ברירת מחדל מקומית / Render עם דיסק (`output/loads.db`).

### חיבור Postgres ב-Vercel (חינם, בלחיצות)

1. בפרויקט ב-Vercel לך ללשונית **Storage** → **Create Database** → בחר
   **Postgres** (Neon) → **Create**. חבר אותו לפרויקט (**Connect Project**).
2. Vercel מזריק אוטומטית את משתני החיבור (כולל `POSTGRES_URL`) למשתני הסביבה
   של הפרויקט — לא צריך להעתיק כלום ידנית.
3. עשה **Redeploy** (Deployments → ⋯ → Redeploy). מרגע זה היסטוריית הטעינות
   נשמרת לצמיתות במסד ה-Postgres, ושורדת פריסות והפעלות מחדש.

> אם אתה מעדיף ספק אחר (למשל Neon/Supabase ישירות) — פשוט הוסף משתנה סביבה בשם
> `DATABASE_URL` עם מחרוזת החיבור, ו-Redeploy.

## מגבלות ידועות ב-Vercel

- **היסטוריית טעינות:** כשמחובר Postgres (ראה מעלה) ההיסטוריה והקבצים נשמרים
  לצמיתות. **בלי** Postgres הם נשמרים ב-`/tmp` הזמני ו"נעלמים" בין הפעלות.
- **מצב ריצה של הטבלה (RUNS):** מצב-הביניים בין טעינת הטבלה להפקה נשמר בזיכרון
  התהליך, ולכן בקבצים גדולים / כמה משתמשים במקביל ייתכן שיאבד ב-serverless.
- **קבצים גדולים:** מצב "קובץ גדול" (ששומר שורות תקינות בשרת) פחות אמין ב-Vercel.
- **זמן התחלה (Cold start):** הכניסה הראשונה אחרי חוסר-שימוש עלולה לקחת מספר שניות
  (pandas כבד לטעינה).

לשימוש יומיומי יציב — הרצה מקומית (`file-loader/TERMINAL.md`) או Render
(`file-loader/DEPLOY.md`) עדיפים.
